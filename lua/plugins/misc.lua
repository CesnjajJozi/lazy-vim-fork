return {
  -- 1. **vim-tmux-navigator** — Seamless navigation between Neovim splits and tmux panes using `<C-h/j/k/l>`.
  { "christoomey/vim-tmux-navigator" },
  -- 2. **vim-fugitive** — Git integration inside Neovim. Lets you run Git commands (`:Git`, `:Gdiff`, `:Gblame`, etc.) without leaving the editor.
  { "tpope/vim-fugitive" },
  -- 3. **nvim-colorizer.lua** — Highlights color codes (hex, RGB, CSS names, etc.) inline in the buffer with their actual color.
  { "NvChad/nvim-colorizer.lua", opts = {} },
}
